spring:
  application:
    name: Synamyk
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/synamyk}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:1111}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
  flyway:
    enabled: false
    locations: classpath:db/migration
    baseline-on-migrate: false

server:
  port: 8080

finik:
  api-key: LOlnU70w7LtRpBZ5HAxLaVatjIXQLGS9xmeSmWM4
  account-id: d54479fa-bea8-4762-ba96-6ec8b1eaea26
  private-key-path: classpath:workkg.pem
  base-url: https://api.acquiring.averspay.kg
  webhook-url: ${APP_BASE_URL}/api/webhooks/finik

jwt:
  secret: ${JWT_SECRET:SynamykSuperSecretKeyForJwtTokenGenerationMinimum256BitsLong}
  expiration: ${JWT_EXPIRATION:86400000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:2592000000}

sms:
  smspro:
    enabled: ${SMS_ENABLED:true}
    url: ${SMSPRO_URL:https://smspro.nikita.kg/api/message}
    login: ${SMSPRO_LOGIN:Cholpon123456}
    password: ${SMSPRO_PASSWORD:6EX5vJf8}
    sender: ${SMSPRO_SENDER:BilgirimKG}
    ttl-minutes: ${SMSPRO_TTL:5}

anthropic:
  api-key: ${ANTHROPIC_API_KEY:your-anthropic-api-key}
  base-url: https://api.anthropic.com

minio:
  url: ${MINIO_URL:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin}
  bucket: ${MINIO_BUCKET:synamyk}
  region: ${MINIO_REGION:us-east-1}

logging:
  level:
    synamyk: DEBUG
    org.springframework.security: WARN